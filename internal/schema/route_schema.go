package schema

const (
	Route string = "route"
)

const (
	Route_id            string = "id"
	Route_fk_company_id string = "fk_company_id"
	Route_name          string = "name"
	Route_note          string = "note"
	Route_is_active     string = "is_active"
)
